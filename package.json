{
  "name": "react-bpmn",
  "private": true,
  "version": "0.0.0",
  "description": "Modelador web de procesos de negocio (monorepo: client + server)",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "lint": "npm --prefix server run lint && npm --prefix client run lint",
    "test": "npm --prefix server run test && npm --prefix client run test",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}